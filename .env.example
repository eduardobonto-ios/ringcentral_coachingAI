# Supabase project settings — Project Settings -> API in the Supabase dashboard.
# Safe to expose to the browser (protected by Row Level Security, see supabase/schema.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
